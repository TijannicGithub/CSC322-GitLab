1.
mkdir GitLab
cd GitLab
2.  echo '#include <stdio.h>\nint main(){ printf("Hello World!\\n"); return 0; }' > HelloWorld.c
3. git init
git add HelloWorld.c
git commit -m "Initial commit: Hello World!"
4. git remote add origin https://github.com/<your-username>/CSC322-GitLab.git
git branch -M main
git push -u origin main
5. echo '#include <stdio.h>\nint main(){ printf("Hello Crool World!\\n"); return 0; }' > HelloWorld.c
git add HelloWorld.c
git commit -m "Update message to 'Hello Crool World!'"
git push
6. git branch Happy
git checkout Happy
git push -u origin Happy
echo '#include <stdio.h>\nint main(){ printf("Hello Happy World!\\n"); return 0; }' > HelloWorld.c
git add HelloWorld.c
git commit -m "Change message to 'Hello Happy World!'"
git push

7. git checkout main
echo '#include <stdio.h>\nint main(){ printf("Hello Very Crool World!\\n"); return 0; }' > HelloWorld.c
git add HelloWorld.c
git commit -m "Update message to 'Hello Very Crool World!'"
git push

8. git merge Happy
